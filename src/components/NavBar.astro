<nav>
  <a href="#">keramikkstudio</a>
  <span class="plus">+</span>
  <a href="#">snekkerverksted</a>
  <span class="plus">+</span>
  <a href="#">coworking</a>
</nav>

<style>
  nav {
    text-align: center;
    display: flex;
    flex-direction: column;
    font-size: 1.4rem;
  }
  span {
    user-select: none;
  }
  @media screen and (min-width: 34em) {
    nav {
      font-size: 2rem;
    }
  }
  @media screen and (min-width: 50em) {
    nav {
      flex-direction: row;
      gap: 1rem;
    }
    .logo {
      height: 200px;
      width: 200px;
    }

    .footer {
      font-size: 3rem;
    }
  }

  a {
    position: relative;
  }

  a::before {
    content: '';
    position: absolute;
    width: 100%;
    height: 2px;
    border-radius: 2px;
    background-color: currentColor;
    bottom: 0;
    left: 0;
    transform-origin: right;
    transform: scaleX(0);
    transition: transform 0.3s ease-in-out;
  }

  a:hover::before {
    transform-origin: left;
    transform: scaleX(1);
  }
</style>
